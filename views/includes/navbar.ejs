<nav class="navbar navbar-expand-md sticky-top">
  <div class="container-fluid px-3">

    <!-- Brand -->
    <a class="navbar-brand" href="/listings">
      <div class="brand-icon">
        <i class="fa-solid fa-compass-drafting"></i>
      </div>
      <span class="brand-name">Wander<span>Nest</span></span>
    </a>

    <!-- Mobile: show search icon + toggler side by side -->
    <div class="d-flex d-md-none align-items-center gap-2 ms-auto">
     
      <button class="navbar-toggler border-0 shadow-none p-1" type="button"
        data-bs-toggle="collapse" data-bs-target="#mainNav"
        aria-controls="mainNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
    </div>

    <div class="collapse navbar-collapse" id="mainNav">

      <!-- Search bar (desktop center / mobile full-width) -->
      <div class="search-wrap mx-md-auto my-2 my-md-0">
        <form class="search-bar" role="search" action="/listings" method="GET">
          <div class="input-with-icon">
            <input class="form-control search-inp" type="search" name="search"
              placeholder="Search destinations…" autocomplete="off">
          </div>
          <button class="btn btn-search" type="submit">Search</button>
        </form>
      </div>

      <!-- Right nav -->
      <div class="navbar-nav align-items-md-center gap-1 ms-md-2 nav-actions">
        <% if (currUser) { %>
          <a class="nav-link btn-nav-add" href="/listings/new">
            <i class="fa-solid fa-plus me-1"></i>List a Stay
          </a>
          <span class="nav-greeting d-none d-md-inline">
            Hi, <%= currUser.username %>
          </span>
          <a class="nav-link btn-nav-login" href="/logout">Log Out</a>
        <% } else { %>
          <a class="nav-link nav-link-plain" href="/listings/new">List a Stay</a>
          <a class="nav-link btn-nav-login" href="/login">Log In</a>
          <a class="nav-link btn-nav-signup" href="/signup">Sign Up</a>
        <% } %>
      </div>

    </div>
  </div>
</nav>
